# Используем официальный образ PostgreSQL версии 13 в качестве базового
FROM postgres:13

# Копируем файл init.sql из локальной системы в директорию инициализации базы данных в контейнере
# Этот файл будет автоматически выполнен при первом запуске контейнера,
# что позволяет создать необходимые таблицы и данные в базе данных
COPY init.sql /docker-entrypoint-initdb.d/

