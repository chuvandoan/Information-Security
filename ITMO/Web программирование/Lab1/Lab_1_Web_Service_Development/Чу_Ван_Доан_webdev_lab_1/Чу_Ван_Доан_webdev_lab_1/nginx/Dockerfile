# Используем последний официальный образ Nginx в качестве базового
FROM nginx:latest

# Копируем файл конфигурации nginx.conf из локальной системы в директорию конфигурации Nginx в контейнере
# Этот файл содержит настройки для веб-сервера Nginx, такие как маршрутизация запросов, обработка статических файлов и другие параметры
COPY nginx.conf /etc/nginx/nginx.conf

